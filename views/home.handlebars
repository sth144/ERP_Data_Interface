<!doctype html>
<html>
  <body onload="">
  	<div class="jumbotron">
  		<h1>bioERP Data Interface</h1>
  	</div>

  	<h1 id="model-header">{{model}}</h1>

  	<div id="tableForms">
  		Table Forms Here
  	</div>

    <table class="table" id="tab">
    	<thead>
    		<!-- Dynamic column headers using number of keys in result -->
    		{{#if results.[0]}}
 		   		{{#with results.[0]}}
 		   			{{#each this}}
 		   				<th>{{@key}}</th>
 		   			{{/each}}
 		   		{{/with}}
 		   	{{/if}}
    	</thead>
	    <tbody>
	      {{#each results}}
	      	<tr>
	      		{{#each this}}
	      			<td>{{this}}</td>
	      		{{/each}}
	      		<td>
	      				<input type="hidden" name="id" value="{{id}}">
	      				<button class="deleteRow">Delete</button>
	
	      		</td>
	      	</tr>
	      {{/each}}
	    </tbody>
	 </table>
  </body>
</html>
